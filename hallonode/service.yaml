apiVersion: v1
kind: Service
metadata:
  name: hallo-node-service
  labels:
    run: hallo-node
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30001
    protocol: TCP
    name: http
  selector:
    run: hallo-node
